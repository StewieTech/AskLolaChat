import React from 'react';
        import { render } from '@testing-library/react';
        import ProfileScreen from './ProfileScreen';
        
        test('renders ProfileScreen component', () => {
            render(<ProfileScreen />);
        });